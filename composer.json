{
    "require": {
        "ext-zmq": "*",
        "psy/psysh": "^0.10.5",
        "ramsey/uuid": "^4.1",
        "react/zmq": "^0.4.0"
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true
    },
    "autoload": {
        "psr-4": {
            "JupyterPhpKernel\\": "src/"
        }
    }
}
